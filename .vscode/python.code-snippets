"Clases de figuras geométricas": {
    "scope": "python",
    "prefix": "clasefiguras",
    "body": [
        "# Elaborar un algoritmo que permita elegir entre 3 figuras: círculo, cuadrilátero o triángulo rectángulo.",
        "# Dependiendo de la figura elegida, solicitará el radio, los lados del cuadrilátero o la base y altura respectivamente.",
        "# Una vez ingresados los datos, deberá mostrar el perímetro y área de la figura elegida.",
        "# Finalmente, consultará si desea realizar otro cálculo y, de responder sí, deberá elegir nuevamente la figura.",
        "# Trabajar el algoritmo aplicando clases y métodos.",
        "",
        "import math",
        "",
        "",
        "class Circulo:",
        "    def __init__(self, radio):",
        "        self.radio = radio",
        "",
        "    def area(self):",
        "        return math.pi * (self.radio ** 2)",
        "",
        "    def perimetro(self):",
        "        return 2 * math.pi * self.radio",
        "",
        "",
        "class Cuadrilatero:",
        "    def __init__(self, lado1, lado2):",
        "        self.lado1 = lado1",
        "        self.lado2 = lado2",
        "",
        "    def area(self):",
        "        return self.lado1 * self.lado2",
        "",
        "    def perimetro(self):",
        "        return 2 * (self.lado1 + self.lado2)",
        "",
        "",
        "class Triangulo_rectangulo:",
        "    def __init__(self, base, altura):",
        "        self.base = base",
        "        self.altura = altura",
        "",
        "    def area(self):",
        "        return (self.base * self.altura) / 2",
        "",
        "    def perimetro(self):",
        "        hipotenusa = math.sqrt(self.base ** 2 + self.altura ** 2)",
        "        return self.base + self.altura + hipotenusa",
        "",
        "",
        "while True:",
        "    print(\"======= DATOS DE FIGURA =======\")",
        "    print(\"Seleccione una figura: \")",
        "    print(\"1. Círculo\")",
        "    print(\"2. Cuadrilátero\")",
        "    print(\"3. Triángulo rectángulo\")",
        "    print(\"=\" * 30)",
        "",
        "    numero_figura = input(\"Ingrese el número de la figura elegida: \")",
        "",
        "    if numero_figura == \"1\":",
        "        radio = float(input(\"Ingrese el radio del círculo: \"))",
        "        circulo = Circulo(radio)",
        "        print(f\"Área del círculo     : {circulo.area():.2f}\")",
        "        print(f\"Perímetro del círculo: {circulo.perimetro():.2f}\")",
        "",
        "    elif numero_figura == \"2\":",
        "        lado1 = float(input(\"Ingrese el primer lado del cuadrilátero: \"))",
        "        lado2 = float(input(\"Ingrese el segundo lado del cuadrilátero: \"))",
        "        cuadrilatero = Cuadrilatero(lado1, lado2)",
        "        print(f\"Área del cuadrilátero     : {cuadrilatero.area():.2f}\")",
        "        print(f\"Perímetro del cuadrilátero: {cuadrilatero.perimetro():.2f}\")",
        "",
        "    elif numero_figura == \"3\":",
        "        base = float(input(\"Ingrese la base del triángulo rectángulo: \"))",
        "        altura = float(input(\"Ingrese la altura del triángulo rectángulo: \"))",
        "        triangulo = Triangulo_rectangulo(base, altura)",
        "        print(f\"Área del triángulo rectángulo     : {triangulo.area():.2f}\")",
        "        print(f\"Perímetro del triángulo rectángulo: {triangulo.perimetro():.2f}\")",
        "",
        "    else:",
        "        print(\"Opción no válida. Elija nuevamente.\")",
        "        continue",
        "",
        "    continuar = input(\"¿Desea realizar otro cálculo? (si/no): \")",
        "",
        "    if continuar.lower() != \"si\":",
        "        break"
    ],
    "description": "Calcula el área y perímetro de tres figuras utilizando clases y métodos"
}
